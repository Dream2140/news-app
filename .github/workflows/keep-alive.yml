name: Keep Fly.io Alive

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Ping API
        run: curl -sf --max-time 30 "https://newsapp-api.fly.dev/api/news/get-all-news?limit=1" > /dev/null
